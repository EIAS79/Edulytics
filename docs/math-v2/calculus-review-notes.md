# Calculus V2 review invariants

- Solver output must remain exact and bounded.
- Verifier must recompute from the original `DerivativeNode` / `IntegralNode` independently of solver traces.
- Unsupported or malformed expressions must fail closed.
- No learner-facing routing is enabled by this slice.
- No curriculum mapping or official outcome code is inferred by this slice.
